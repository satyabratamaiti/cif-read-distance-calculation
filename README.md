# cif-read-distance-calculation
#This code takes the base pair information and corresponding cif file name from a csv file. It calculates C1'-C1' distances of specific basepair from one or many cif file.
